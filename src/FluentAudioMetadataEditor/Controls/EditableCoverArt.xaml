<?xml version="1.0" encoding="utf-8"?>
<UserControl mc:Ignorable="d"
             x:Class="FluentAudioMetadataEditor.Controls.EditableCoverArt"
             x:DefaultBindMode="OneWay"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="LayoutRoot"
          BorderBrush="{StaticResource ControlElevationBorderBrush}"
          BorderThickness="1"
          CornerRadius="8">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="CommonStates">
                <VisualState x:Name="Normal"/>

                <!--
                <VisualState x:Name="PointerEntered">
                    <VisualState.Setters>
                        <Setter Property="EditIndicationOverlayGrid.Visibility" Value="Visible"/>
                    </VisualState.Setters>
                </VisualState>
                -->
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
        <Grid.Background>
            <ImageBrush ImageSource="{x:Bind ImageSource}"/>
        </Grid.Background>
        
        <Grid x:Name="EditIndicationOverlayGrid"
              Background="#88000000"
              CornerRadius="8"
              Visibility="Collapsed">
            <FontIcon Glyph="&#xec4f;" FontSize="64" Visibility="Collapsed"/>
        
            <Button Height="64"
                    HorizontalAlignment="Center"
                    Width="64"
                    VerticalAlignment="Center">
                <FontIcon Glyph="&#xe70f;" FontSize="32"/>
            </Button>
        </Grid>
    </Grid>
</UserControl>